server:
  port: 8080
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
      - id: gateway
        uri: http://www.ityouknow.com
        predicates:
        - Path=/spring-cloud